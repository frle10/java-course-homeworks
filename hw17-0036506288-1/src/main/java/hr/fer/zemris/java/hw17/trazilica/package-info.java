/**
 * This package contains classes used in implementation of a simple
 * search engine that can search text files.
 * <p>
 * The engine can be used through a simple custom shell that offers
 * several commands with which a user can, among other things, ask
 * for a query and the engine will return several documents closest
 * or most similar to the query string.
 */
package hr.fer.zemris.java.hw17.trazilica;